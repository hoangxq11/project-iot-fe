export class LightMode {
    id!: string;
    mode!: string;
}

export class FanMode {
    id!: string;
    mode!: string;
}

export class ManualLight {
    id!: string;
    mode!: string;
}

export class RelayMode {
    id!: string;
    mode!: string;
}